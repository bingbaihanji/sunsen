# Hello plugin sample config (deployed at pluginsDir/<pluginId>/config.properties)
# Read via PluginProperties; parse failures silently fall back to defaults.
# NOTE: java.util.Properties#load(InputStream) treats the file as ISO-8859-1.
#       Use ASCII-safe values here, or \\uXXXX escapes for non-ASCII.

greeting.suffix= !!
log.level=info
tags=demo, hello, sunsen
